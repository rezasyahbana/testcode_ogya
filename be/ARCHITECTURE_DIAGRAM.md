# Data Generator - Architecture Visualization

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     USER                                     │
│                       │                                      │
│                       ▼                                      │
│              request-conf.json                               │
│               (Configuration)                                │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                  cmd/app/main.go                             │
│                  (Entry Point)                               │
│                                                              │
│  1. Load config from JSON                                   │
│  2. Create Generator instance                               │
│  3. Start generation process                                │
│  4. Display performance metrics                             │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│            internal/generator/generator.go                   │
│                (Main Engine)                                 │
│                                                              │
│  ┌──────────────────────────────────────────────┐          │
│  │  1. Detect CPU cores: runtime.NumCPU()       │          │
│  │  2. Create worker pool (N = cores)           │          │
│  │  3. Split rows: total / workerCount          │          │
│  │  4. Launch N goroutines                      │          │
│  │  5. Collect results via buffered channel     │          │
│  │  6. Write to file (CSV/JSON/SQL)             │          │
│  └──────────────────────────────────────────────┘          │
└───────────────────────┬─────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│  Worker 1   │ │  Worker 2   │ │  Worker N   │
│             │ │             │ │             │
│ Rows:       │ │ Rows:       │ │ Rows:       │
│ 0-83K       │ │ 83K-166K    │ │ 916K-999K   │
│             │ │             │ │             │
│ ┌─────────┐ │ │ ┌─────────┐ │ │ ┌─────────┐ │
│ │Generate │ │ │ │Generate │ │ │ │Generate │ │
│ │Person   │ │ │ │Person   │ │ │ │Person   │ │
│ └────┬────┘ │ │ └────┬────┘ │ │ └────┬────┘ │
│      │      │ │      │      │ │      │      │
│      ▼      │ │      ▼      │ │      ▼      │
│ ┌─────────┐ │ │ ┌─────────┐ │ │ ┌─────────┐ │
│ │Map to   │ │ │ │Map to   │ │ │ │Map to   │ │
│ │Columns  │ │ │ │Columns  │ │ │ │Columns  │ │
│ └────┬────┘ │ │ └────┬────┘ │ │ └────┬────┘ │
└──────┼──────┘ └──────┼──────┘ └──────┼──────┘
       │               │               │
       └───────────────┼───────────────┘
                       │
                       ▼
         ┌─────────────────────────┐
         │  Buffered Channel       │
         │  (cap: 120)             │
         │                         │
         │  Thread-safe queue      │
         └───────────┬─────────────┘
                     │
                     ▼
         ┌─────────────────────────┐
         │   File Writer           │
         │   (Sequential)          │
         │                         │
         │   ▼ data.csv            │
         │   ▼ data.json           │
         │   ▼ data.sql            │
         └─────────────────────────┘
```

## 🔄 Person Generation Flow (Correlation Logic)

```
┌──────────────────────────────────────────────────────────┐
│     internal/generator/datasource.go                     │
│                                                          │
│                 GeneratePerson()                         │
│                                                          │
│  Step 1: Select Gender                                  │
│  ┌────────────────────────────────┐                    │
│  │  random(2)                      │                    │
│  │   └─► 0: gender = "P" (Female) │                    │
│  │   └─► 1: gender = "L" (Male)   │                    │
│  └────────────┬───────────────────┘                    │
│               │                                          │
│               ▼                                          │
│  Step 2: Pick First Name                                │
│  ┌────────────────────────────────┐                    │
│  │  if gender == "L":              │                    │
│  │    firstname = random(          │                    │
│  │      firstname_male.csv)        │                    │
│  │  else:                          │                    │
│  │    firstname = random(          │                    │
│  │      firstname_female.csv)      │                    │
│  └────────────┬───────────────────┘                    │
│               │                                          │
│               ▼                                          │
│  Step 3: Pick Last Name                                 │
│  ┌────────────────────────────────┐                    │
│  │  lastname = random(             │                    │
│  │    lastname.csv)                │                    │
│  └────────────┬───────────────────┘                    │
│               │                                          │
│               ▼                                          │
│  Step 4: Optional Middle Name                           │
│  ┌────────────────────────────────┐                    │
│  │  if random(100) < 30:           │                    │
│  │    middlename = random(         │                    │
│  │      firstname OR lastname)     │                    │
│  └────────────┬───────────────────┘                    │
│               │                                          │
│               ▼                                          │
│  Step 5: Construct Full Name                            │
│  ┌────────────────────────────────┐                    │
│  │  fullname = firstname           │                    │
│  │           + " " + middlename    │                    │
│  │           + " " + lastname      │                    │
│  └────────────┬───────────────────┘                    │
│               │                                          │
│               ▼                                          │
│  Step 6: Return Person Entity                           │
│  ┌────────────────────────────────┐                    │
│  │  Person {                       │                    │
│  │    Gender:     "P"              │                    │
│  │    FirstName:  "Siti"           │                    │
│  │    MiddleName: "Ratna"          │                    │
│  │    LastName:   "Wijaya"         │                    │
│  │    FullName:   "Siti Ratna      │                    │
│  │                 Wijaya"          │                    │
│  │  }                              │                    │
│  └─────────────────────────────────┘                    │
└──────────────────────────────────────────────────────────┘
```

## 📦 Data Embedding

```
┌────────────────────────────────────────────┐
│  internal/generator/datasource.go          │
│                                            │
│  //go:embed assets/firstname_male.csv     │
│  var firstnameMaleCSV string              │
│                                            │
│  //go:embed assets/firstname_female.csv   │
│  var firstnameFemaleCSV string            │
│                                            │
│  //go:embed assets/lastname.csv           │
│  var lastnameCSV string                   │
└────────────────┬───────────────────────────┘
                 │
                 │ (At compile time)
                 ▼
┌────────────────────────────────────────────┐
│         Compiled Binary                    │
│    (generator-linux / generator-win.exe)   │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  Executable Code                     │ │
│  │  +                                   │ │
│  │  Embedded CSV Data                   │ │
│  │  ├─ 40 male names                    │ │
│  │  ├─ 40 female names                  │ │
│  │  └─ 40 surnames                      │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  Size: 2.8 MB (standalone)                │
│  Dependencies: NONE                        │
└────────────────────────────────────────────┘
```

## ⚙️ Configuration Flow

```
request-conf.json
       │
       ▼
┌─────────────────────┐
│  JSON Parser        │
│                     │
│  Unmarshal into:    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────────────────────┐
│  entity.Config                      │
│                                     │
│  ├─ GlobalSettings {                │
│  │    RowCount: 1000000            │
│  │    OutputFormat: "sql"          │
│  │    FileName: "data_2024"        │
│  │  }                              │
│  │                                 │
│  ├─ SQLSettings {                  │
│  │    Dialect: "postgresql"        │
│  │    TableName: "public.users"    │
│  │  }                              │
│  │                                 │
│  └─ Columns: [                     │
│       {                            │
│         ColumnName: "id"           │
│         GeneratorType: "uuid"      │
│       },                           │
│       {                            │
│         ColumnName: "full_name"    │
│         GeneratorType: "full_name" │
│       },                           │
│       ...                          │
│     ]                              │
└─────────────────────────────────────┘
```

## 🎯 Column Mapping (Correlation Ensured)

```
For each row:

1. Generate Person ONCE
   ┌──────────────────┐
   │  Person {        │
   │    Gender: "P"   │
   │    FirstName:    │
   │      "Siti"      │
   │    FullName:     │
   │      "Siti       │
   │       Wijaya"    │
   │  }               │
   └────────┬─────────┘
            │
            ▼
2. Map to ALL requested columns from SAME person

   Column: "full_name"  → person.FullName  → "Siti Wijaya"
   Column: "first_name" → person.FirstName → "Siti"
   Column: "gender"     → person.Gender    → "P"
   Column: "email"      → person.FirstName + domain → "Siti@gmail.com"

   ✅ ALL VALUES CORRELATED within this row
```

## 🚀 Performance Optimization

```
Sequential (1 core):
  ┌────┐
  │ CPU│ ████████████████ (100%)
  └────┘
  └─► 18,000 rows/sec

Parallel (12 cores):
  ┌────┐
  │CPU1│ ██████████████
  │CPU2│ ██████████████
  │CPU3│ ██████████████
  │CPU4│ ██████████████
  │CPU5│ ██████████████
  │CPU6│ ██████████████
  │CPU7│ ██████████████
  │CPU8│ ██████████████
  │CPU9│ ██████████████
  │CP10│ ██████████████
  │CP11│ ██████████████
  │CP12│ ██████████████
  └────┘
  └─► 208,000 rows/sec (11.5x speedup!)
```

---

**Visualization Complete** ✅
